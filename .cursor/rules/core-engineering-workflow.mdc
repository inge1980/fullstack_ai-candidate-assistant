---
description: Core engineering workflow
alwaysApply: true
---

# Core engineering workflow

- Follow the existing architecture before introducing new patterns.
- Make the smallest change that solves the task.
- Do not modify unrelated files.
- Explain trade-offs before major architectural changes.
- Consider security, error handling, performance and maintainability.
- When requirements are ambiguous, identify the ambiguity before implementing.
- Suggest tests for new business logic.
- Never assume generated code is correct; explain why it works.
- Create and edit text files as UTF-8 without BOM so Scandinavian letters stay intact (æ, ø, å, Æ, Ø, Å). Do not write source, Markdown, JSON, prompts, or i18n as Windows-1252, UTF-16, or UTF-8 with BOM.
- When creating files through the Windows shell, set encoding explicitly (PowerShell utf8NoBOM); do not rely on the console default. Binary files are unchanged.
